server {
  listen ${PORT};
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  # En Render el bundle usa URLs absolutas (CORS directo), Nginx solo estáticos
  location / {
    try_files $uri $uri/ /index.html;
  }
}